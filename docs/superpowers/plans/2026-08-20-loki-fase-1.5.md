# Loki Fase 1.5 — piano di implementazione

> **Per chi esegue con agenti:** SUB-SKILL RICHIESTA: usare `superpowers:subagent-driven-development`
> (consigliata) oppure `superpowers:executing-plans` per eseguire questo piano task per task.
> Gli step usano checkbox (`- [ ]`) per il tracciamento.

**Goal:** dare all'hub un log store privato, interrogabile da Grafana, che risponda a
*"quale tool call è fallita in quella sessione"* — senza toccare i tre numeri pubblici.

**Architettura:** Claude Code → ingest OTLP esistente → una pipeline `logs` **nuova e
separata** nel Collector → Loki (single binary su Railway) → chunk e index su **R2**.
Grafana lo interroga sulla rete privata. Loki non riceve nessun hostname del Tunnel.

**Stack:** OpenTelemetry Collector contrib 0.158.0 · Grafana Loki 3.7.6 (single binary) ·
Cloudflare R2 (S3-compatible) · Railway · Grafana.

**Spec:** `docs/superpowers/specs/2026-08-20-loki-fase-1.5-design.md` — leggerla prima,
in particolare §4 (l'ordine dei lavori) e §5 (le due allow-list).

---

## Mappa dei file

| file | responsabilità | task |
|---|---|---|
| `docker/loki.yaml` | **creato** — l'unica copia della config di Loki, per locale e produzione | 3 |
| `railway/loki/Dockerfile` | **creato** — immagine di produzione, copia `docker/loki.yaml` | 4 |
| `railway/loki/railway.json` | **creato** — builder, `watchPatterns` | 4 |
| `docker/docker-compose.yml` | **modificato** — servizio `loki` + alias `loki.railway.internal` | 5 |
| `docker/otel-collector-config.yaml` | **modificato** — pipeline `logs` nuova, allow-list #1 | 6 |
| `docker/grafana/provisioning/datasources/loki.yml` | **creato** — datasource | 7 |
| `.github/workflows/smoke.yml` | **modificato** — asserzione su `/v1/logs` | 1 |
| `.github/workflows/images.yml` | **modificato** — tre gate nuovi | 8 |
| `docs/LOCAL_DRY_RUN.md` | **modificato** — sezione sui log | 2, 9 |
| `docs/telemetry-baseline.json` | **modificato** — il verbale include i log | 2 |
| `docs/DECISIONS.md`, `SECURITY.md`, `CLAUDE.md`, `README.md`, `docs/BLOCKERS.md` | **modificati** | 10 |

**Un solo esemplare di ogni configurazione, in `docker/`.** L'immagine Railway la copia
a build time, il compose la monta. Non si biforca mai "per farla funzionare in locale".

---

## Ordine, e cosa può andare in parallelo

```
Task 0  (MARCO, infrastruttura)  ─┐
                                  ├─> Task 1  smoke.yml
Task 2  misura (dry run)  ────────┴─> Task 3  loki.yaml ──┐
                                      Task 6  pipeline    ├─> Task 9  prova di sicurezza
Task 4  railway/loki   ─┐                                 │
Task 5  compose        ─┼─ indipendenti, in parallelo ────┘
Task 7  datasource     ─┘
                                                Task 8  gate CI ──> Task 10  docs
```

**Wave parallela A** (nessuna dipendenza): Task 4, 5, 7.
**Wave parallela B** (dopo il Task 2): Task 3, 6.
**Poi in sequenza**: 8, 9, 10.

Il Task 1 dipende dal Task 0 e da nient'altro.

---

## Task 0: la regola WAF — MARCO, non delegabile

**Non è un task da agente.** La regola vive nella dashboard Cloudflare, fuori da git.

- [ ] **Step 1: estendere la regola su `otel.marcobellingeri.dev`**

Oggi lascia passare solo `POST /v1/metrics` con header `Authorization` presente.
Va estesa a `POST /v1/logs` con la stessa condizione sull'header.
**Il valore dell'header non si guarda, mai** — una regola WAF si legge da dashboard e
da API, quindi un token lì dentro è un segreto fuori dal secret store.

- [ ] **Step 2: misurare subito, contro produzione**

```bash
HOST=https://otel.marcobellingeri.dev
# Un token DELIBERATAMENTE invalido, in una variabile: e' la stessa forma che usa
# smoke.yml, e serve a distinguere chi risponde. 401 lo dice solo il Collector,
# 403 solo il bordo — instradare sul percorso invece che sul codice era sbagliato
# gia' una volta, il 20/08 sera.
TOKEN_INVALIDO="questo-token-non-esiste"
POST=(-X POST -H 'Content-Type: application/json' -d '{}')

for p in /v1/metrics /v1/logs; do
  echo -n "$p senza header: "
  curl -sS -o /dev/null -w '%{http_code}\n' "${POST[@]}" "${HOST}${p}"
  echo -n "$p bearer sbagliato: "
  curl -sS -o /dev/null -w '%{http_code}\n' "${POST[@]}" -H "Authorization: Bearer ${TOKEN_INVALIDO}" "${HOST}${p}"
done
echo -n "/v1/traces (deve restare chiuso): "
curl -sS -o /dev/null -w '%{http_code}\n' "${POST[@]}" "${HOST}/v1/traces"
```

Atteso **dopo** la modifica:

```
/v1/metrics senza header: 403      (bordo)
/v1/metrics bearer sbagliato: 401  (Collector)
/v1/logs senza header: 403         (bordo)
/v1/logs bearer sbagliato: 401     (Collector)
/v1/traces: 403                    (bordo, invariato)
```

Se `/v1/logs bearer sbagliato` risponde `403`, la regola non è stata estesa e **il
Task 1 non va fatto**: si fermerebbe un'asserzione vera su un comportamento assente.

---

## Task 1: smoke.yml impara che `/v1/logs` è aperto

**Files:**
- Modify: `.github/workflows/smoke.yml`

- [ ] **Step 1: aggiungere l'asserzione, senza toccare le altre**

In `.github/workflows/smoke.yml`, nel job `ingest-chiede-il-token`, subito **dopo** la
riga `controlla 401 "un bearer non valido" ...`, aggiungere:

```bash
          # `/v1/logs` e' aperto dal 20/08/2026 perche' Loki ingerisce i log event di
          # Claude Code (Fase 1.5). Il bordo lo lascia passare, quindi il 401 lo dice
          # il Collector: e' la prova che l'autenticazione vale sul percorso NUOVO e
          # non solo su quello vecchio. Aprire una rotta e non asserirla e' come
          # aprirla e dimenticarsene.
          controlla 401 "un bearer non valido su /v1/logs" /v1/logs "${post[@]}" \
            -H 'Authorization: Bearer questo-token-non-esiste'
```

- [ ] **Step 2: spostare `/v1/logs` fuori dai `403`, se ci fosse**

Cercare nel file `v1/logs`. Se compare in un `controlla 403 ...`, **va rimosso**: la
stessa riga non può asserire che il bordo blocchi e che il Collector risponda.

```bash
grep -n "v1/logs" .github/workflows/smoke.yml
```

Atteso dopo la modifica: **una sola** occorrenza, quella del passo 1.

- [ ] **Step 3: provarlo ESEGUENDOLO, non leggendolo**

`smoke.yml` non gira sulle PR. Si estrae lo script dal YAML committato e lo si esegue:

```bash
python3 - <<'PY' > /tmp/smoke.sh
import yaml, textwrap
w = yaml.safe_load(open('.github/workflows/smoke.yml'))
s = [x for x in w['jobs']['ingest-chiede-il-token']['steps'] if 'run' in x][0]['run']
open('/tmp/smoke.sh','w').write(textwrap.dedent(s))
PY
HOST=https://otel.marcobellingeri.dev bash /tmp/smoke.sh; echo "exit=$?"
```

Atteso: `exit=0` e sei righe `ok:`.

- [ ] **Step 4: provarlo ROSSO rompendo la realtà, non l'atteso**

Cambiare il **percorso** della richiesta (la realtà), lasciando `401` come atteso:

```bash
sed 's#/v1/logs "\${post\[@\]}"#/v1/traces "${post[@]}"#' /tmp/smoke.sh > /tmp/smoke-rotto.sh
HOST=https://otel.marcobellingeri.dev bash /tmp/smoke-rotto.sh; echo "exit=$?"
```

Atteso: `exit=1`, con il messaggio che nomina la regola WAF (non quello sull'ingest) —
perché il codice atteso è `401` e ne arriva `403`.
**Non** cambiare il valore atteso per farlo fallire: testerebbe il proprio ramo.

- [ ] **Step 5: commit**

```bash
git add .github/workflows/smoke.yml
git commit -m "feat(smoke): l'ingest dei log e' aperto, e adesso c'e' una riga che lo prova"
```

---

## Task 2: misurare cosa manda Claude Code sui log record

**Nessuno sa quali attributi arrivino.** Le allow-list dei Task 3 e 6 si scrivono da
questa misura. Scriverle prima sarebbe indovinare, e su questo progetto un attributo
d'identità è già arrivato una volta dove non doveva.

**Files:**
- Modify: `docker/otel-collector-config.yaml` (temporaneamente, NON si committa)
- Modify: `docs/LOCAL_DRY_RUN.md`
- Modify: `docs/telemetry-baseline.json`

- [ ] **Step 1: pipeline `logs` temporanea verso il debug exporter**

In `docker/otel-collector-config.yaml`, aggiungere sotto `exporters:`:

```yaml
  debug/logs-misura:
    verbosity: detailed
```

e sotto `service.pipelines:`, accanto a `metrics:`:

```yaml
    logs:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [debug/logs-misura]
```

**Nessun allow-list qui**: lo scopo è vedere *tutto* ciò che arriva. Questa modifica è
usa e getta e **non va committata**.

- [ ] **Step 2: avviare lo stack e alimentarlo col client vero**

```bash
cd docker
cat > .env <<EOF
CLOUDFLARE_TUNNEL_TOKEN=unused-locally
GRAFANA_ADMIN_PASSWORD=$(openssl rand -hex 8)
STATUS_API_TOKEN=$(openssl rand -hex 16)
OTLP_INGEST_TOKEN=$(openssl rand -hex 16)
SENTRY_DSN=
EOF
cat > /tmp/ports.yml <<'EOF'
services:
  otel-collector: { ports: ["4318:4318", "8889:8889"] }
EOF
docker compose -f docker-compose.yml -f /tmp/ports.yml up -d --build otel-collector

set -a; . ./.env; set +a
export CLAUDE_CODE_ENABLE_TELEMETRY=1 \
       OTEL_METRICS_EXPORTER=otlp OTEL_LOGS_EXPORTER=otlp \
       OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf \
       OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318 \
       OTEL_EXPORTER_OTLP_HEADERS="Authorization=Bearer $OTLP_INGEST_TOKEN" \
       OTEL_METRIC_EXPORT_INTERVAL=5000 OTEL_LOGS_EXPORT_INTERVAL=5000
claude -p "reply only: ok" --model claude-haiku-4-5-20251001
```

`OTEL_LOGS_EXPORTER=otlp` è la variabile che oggi `docs/CLAUDE_CODE_TELEMETRY.md`
lascia deliberatamente **non impostata**: è quella che accende la metà dei log.

- [ ] **Step 3: leggere gli attributi, tutti**

```bash
docker compose logs otel-collector | grep -A40 "LogsExporter" | tee /tmp/log-record.txt
echo "=== chiavi degli attributi viste ==="
grep -oE '^\s+-> [a-zA-Z._]+:' /tmp/log-record.txt | sed 's/[ ->:]//g' | sort -u
echo "=== identita': deve essere VUOTO ==="
grep -iE "user\.email|user\.id|user\.account|organization\.id" /tmp/log-record.txt || echo "(nessuna occorrenza — da confermare, non da assumere)"
```

- [ ] **Step 4: due sessioni, due `session.id`**

È la metà che la produzione non può auto-verificare:

```bash
claude -p "reply only: a" --model claude-haiku-4-5-20251001 &
claude -p "reply only: b" --model claude-haiku-4-5-20251001 &
wait
docker compose logs otel-collector | grep -oE 'session\.id: Str\([^)]*\)' | sort -u
```

Atteso: **due valori distinti**.

- [ ] **Step 5: scrivere il verbale**

Aggiornare `docs/telemetry-baseline.json` aggiungendo una chiave, **senza toccare le
altre**:

```json
  "_log_record_attributes": "<l'elenco esatto misurato allo step 3>",
  "_log_dry_run": "<data di oggi>"
```

Aggiungere a `docs/LOCAL_DRY_RUN.md`, in fondo alla sezione 4, un paragrafo che
riporta l'elenco misurato e la data.

- [ ] **Step 6: annullare la modifica temporanea**

```bash
git checkout docker/otel-collector-config.yaml
docker compose down -v && rm .env
git diff --stat   # devono comparire SOLO telemetry-baseline.json e LOCAL_DRY_RUN.md
```

- [ ] **Step 7: commit**

```bash
git add docs/telemetry-baseline.json docs/LOCAL_DRY_RUN.md
git commit -m "docs(telemetria): misurato cosa Claude Code manda sui log record"
```

---

## Task 3: `docker/loki.yaml`

**Dipende dal Task 2** (l'allow-list si scrive dalla misura).

**Files:**
- Create: `docker/loki.yaml`

- [ ] **Step 1: leggere la doc del fornitore PRIMA di scrivere**

```bash
# La forma esatta di object_store per un endpoint S3 NON-AWS, e il nome del flag
# di verifica della config. Non andare a memoria: qui un errore nasce silenzioso.
```
Consultare la doc Loki su *"storage configuration examples"* e *"validating
configuration"*. Serve confermare tre cose: (a) `use_thanos_objstore: true` con
`object_store.s3` è la forma corrente per un endpoint custom; (b) `bucket_lookup_type:
path` è ciò che serve a R2; (c) il flag di verifica (`-verify-config` o equivalente).
**Scrivere nel commit quale doc dice cosa.**

- [ ] **Step 2: scrivere il file**

```yaml
# Loki — Fase 1.5. Una sola copia di questa configurazione, come ogni altra in
# docker/: l'immagine Railway la copia a build time, il compose la monta.
#
# `auth_enabled: false` NON significa "senza autenticazione": in Loki riguarda il
# multi-tenancy (l'header X-Scope-OrgID), non l'accesso. Cio' che tiene chiuso
# questo servizio e' che non ha nessun hostname del Tunnel, esattamente come
# Prometheus. Non si riscriva la ragione falsa: e' gia' stata corretta una volta
# in SECURITY.md.
auth_enabled: false

server:
  http_listen_port: 3100
  # 0.0.0.0 e non il default: Grafana e il Collector sono altri container. Resta
  # sulla rete privata Railway.
  http_listen_address: 0.0.0.0

common:
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory
  # Single binary: una sola istanza, nessuna replica. La doc lo richiede
  # esplicitamente per deploymentMode SingleBinary.
  replication_factor: 1
  path_prefix: /loki

schema_config:
  configs:
    - from: 2026-08-20
      store: tsdb
      object_store: s3
      schema: v13
      index:
        prefix: index_
        period: 24h

storage_config:
  use_thanos_objstore: true
  tsdb_shipper:
    active_index_directory: /loki/index
    cache_location: /loki/index_cache
    cache_ttl: 24h
  object_store:
    s3:
      bucket_name: ${LOKI_R2_BUCKET}
      # Endpoint R2: <ACCOUNT_ID>.r2.cloudflarestorage.com, SENZA schema.
      endpoint: ${LOKI_R2_ENDPOINT}
      access_key_id: ${LOKI_R2_ACCESS_KEY_ID}
      secret_access_key: ${LOKI_R2_SECRET_ACCESS_KEY}
      # R2 vuole region `auto`.
      region: auto
      bucket_lookup_type: path
      # insecure resta FALSE: l'endpoint R2 e' HTTPS. Metterlo a true qui
      # significherebbe spedire le credenziali in chiaro verso internet.
      insecure: false

limits_config:
  # ALLOW-LIST #2, ed e' la ragione per cui ne servono due.
  #
  # Il DEFAULT di Loki e' il caso peggiore: le resource attribute diventano label
  # di stream e le restanti finiscono in structured metadata. Cioe' `user.email`
  # DENTRO L'INDICE, non solo dentro il record. `ignore_defaults: true` ribalta la
  # cosa in default-deny, e regge anche se qualcuno sbaglia l'allow-list del
  # Collector.
  otlp_config:
    resource_attributes:
      ignore_defaults: true
      attributes_config:
        - action: index_label
          attributes: [service.name]
    log_attributes:
      # <ESITO DEL TASK 2>: qui vanno le chiavi misurate che vogliamo poter
      # filtrare — session.id e model sono le due attese. Structured metadata e
      # NON label: in Prometheus session.id e' obbligatoria, qui creerebbe uno
      # stream per sessione, che e' il primo anti-pattern di cardinalita' che la
      # doc nomina.
      - action: structured_metadata
        attributes: [session.id, model]
      # Tutto il resto cade. Non e' una delete-list: e' l'ultima riga di un
      # allow-list, e vale su ogni attributo che una versione futura inventa.
      - action: drop
        regex: .*
  retention_period: 720h

compactor:
  working_directory: /loki/compactor
  # Senza `delete_request_store` la retention non viene applicata e i chunk
  # restano su R2 per sempre: il tetto di 30 giorni sarebbe una dichiarazione,
  # non un limite.
  delete_request_store: s3
  retention_enabled: true
```

- [ ] **Step 3: verificare la config con il binario vero, non a occhio**

```bash
docker run --rm -v "$PWD/docker/loki.yaml:/etc/loki/loki.yaml:ro" \
  -e LOKI_R2_BUCKET=finto -e LOKI_R2_ENDPOINT=finto.r2.cloudflarestorage.com \
  -e LOKI_R2_ACCESS_KEY_ID=finto -e LOKI_R2_SECRET_ACCESS_KEY=finto \
  grafana/loki:3.7.6 -config.file=/etc/loki/loki.yaml -config.expand-env=true -verify-config
```

Atteso: exit 0. Se il flag non esiste con questo nome, **è lo Step 1 che è stato
saltato** — tornare alla doc, non tentare varianti a caso.

- [ ] **Step 4: commit**

```bash
git add docker/loki.yaml
git commit -m "feat(loki): la config, con l'allow-list che ribalta il default del fornitore"
```

---

## Task 4: `railway/loki/` — indipendente, parallelizzabile

**Files:**
- Create: `railway/loki/Dockerfile`
- Create: `railway/loki/railway.json`

- [ ] **Step 1: il Dockerfile**

```dockerfile
# checkov:skip=CKV_DOCKER_2: Railway ignora l'HEALTHCHECK del Dockerfile — usa il suo
# healthcheckPath, che qui NON e' dichiarato (vedi DECISIONS.md: e' una decisione
# chiusa per i servizi senza rotta pubblica).
# Pinnata per DIGEST oltre che per tag: un tag e' un'etichetta e si puo' ripuntare
# sotto lo stesso nome.
FROM grafana/loki:3.7.6@sha256:efd47c67f9bac88ca29bcf8cb997d9ab29d1848bd0aff579282295542a745952

# 10001 e' l'utente che l'immagine di Loki usa gia'.
USER 10001:10001

COPY docker/loki.yaml /etc/loki/loki.yaml

# -config.expand-env: le credenziali R2 arrivano da variabili di servizio Railway,
# mai dal file. Senza questo flag Loki leggerebbe la stringa `${LOKI_R2_BUCKET}`
# alla lettera e fallirebbe la connessione con un errore che non nomina la causa.
CMD ["-config.file=/etc/loki/loki.yaml", "-config.expand-env=true"]
```

Il digest sopra è stato letto il 2026-08-20 con `docker inspect`, non copiato da un
esempio. **Ricontrollarlo prima di eseguire**: se `3.7.6` non è più l'ultima 3.x, va
rifatto il giro, perché un digest vecchio pinna un'immagine vecchia in silenzio.

```bash
curl -sS "https://api.github.com/repos/grafana/loki/releases?per_page=10" | \
  python3 -c "import json,sys; [print(r['tag_name'], r['published_at'][:10]) for r in json.load(sys.stdin)]"
docker pull grafana/loki:<tag scelto>
docker inspect --format='{{index .RepoDigests 0}}' grafana/loki:<tag scelto>
```

- [ ] **Step 2: `railway.json`**

```json
{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "railway/loki/Dockerfile",
    "watchPatterns": [
      "docker/loki.yaml",
      "railway/loki/**"
    ]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE"
  }
}
```

`docker/loki.yaml` **deve** stare nei `watchPatterns`: un `COPY` non coperto significa
che Railway non ricostruisce il servizio quando quel file cambia — la modifica entra in
`main` verde e la produzione resta indietro in silenzio. C'è già un gate in
`images.yml` che lo verifica, e questo file lo deve soddisfare.

- [ ] **Step 3: verificare che il gate dei `watchPatterns` sia contento**

```bash
python3 - <<'PY'
import json, re, pathlib
rj = json.load(open('railway/loki/railway.json'))
pattern = rj['build']['watchPatterns']
copiati = re.findall(r'^COPY\s+(\S+)', pathlib.Path('railway/loki/Dockerfile').read_text(), re.M)
print("COPY:", copiati)
print("watchPatterns:", pattern)
for c in copiati:
    assert any(c == p or c.startswith(p.rstrip('*')) for p in pattern), f"{c} non coperto"
print("ok: ogni COPY e' coperto")
PY
```

- [ ] **Step 4: la build passa**

```bash
docker build -f railway/loki/Dockerfile -t agentic-loki:prova .
```

- [ ] **Step 5: commit**

```bash
git add railway/loki/
git commit -m "feat(loki): immagine di produzione, con docker/loki.yaml nei watchPatterns"
```

---

## Task 5: il compose locale — indipendente, parallelizzabile

**Files:**
- Modify: `docker/docker-compose.yml`

- [ ] **Step 1: aggiungere il servizio, dopo `grafana:`**

```yaml
  loki:
    image: grafana/loki:3.7.6@sha256:efd47c67f9bac88ca29bcf8cb997d9ab29d1848bd0aff579282295542a745952
    restart: unless-stopped
    networks:
      default:
        aliases: ["loki.railway.internal"]
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    # Nessuna rotta del Tunnel verso questo servizio, mai — stessa regola di
    # Prometheus. `auth_enabled: false` in loki.yaml riguarda il multi-tenancy,
    # non l'accesso: qui non c'e' niente da autenticare perche' non c'e' nessuna
    # rotta pubblica.
    environment:
      - LOKI_R2_BUCKET=${LOKI_R2_BUCKET}
      - LOKI_R2_ENDPOINT=${LOKI_R2_ENDPOINT}
      - LOKI_R2_ACCESS_KEY_ID=${LOKI_R2_ACCESS_KEY_ID}
      - LOKI_R2_SECRET_ACCESS_KEY=${LOKI_R2_SECRET_ACCESS_KEY}
    volumes:
      - ./loki.yaml:/etc/loki/loki.yaml:ro
      - loki-data:/loki
    command: ["-config.file=/etc/loki/loki.yaml", "-config.expand-env=true"]
```

L'alias `loki.railway.internal` è il motivo per cui `docker/loki.yaml` e il datasource
di Grafana non esistono in due versioni: **lo stesso hostname vale nei due ambienti.**

- [ ] **Step 2: il volume**

Nella sezione `volumes:` in fondo al file, accanto a `prometheus-data` e
`grafana-data`, aggiungere:

```yaml
  loki-data:
```

Serve anche con lo storage su R2: `tsdb_shipper` tiene indice attivo e cache su disco
locale prima di spedirli.

- [ ] **Step 3: il compose è valido**

Il gate `compose` in CI esegue esattamente questo:

```bash
LOKI_R2_BUCKET=x LOKI_R2_ENDPOINT=x LOKI_R2_ACCESS_KEY_ID=x LOKI_R2_SECRET_ACCESS_KEY=x \
CLOUDFLARE_TUNNEL_TOKEN=x GRAFANA_ADMIN_PASSWORD=x STATUS_API_TOKEN=x OTLP_INGEST_TOKEN=x SENTRY_DSN=x \
docker compose -f docker/docker-compose.yml config --quiet && echo "compose valido"
```

- [ ] **Step 4: aggiornare il gate `compose` in CI**

Il job passa oggi cinque variabili. Va esteso alle quattro nuove, **altrimenti diventa
rosso al primo run** — cercarle in `.github/workflows/images.yml` e aggiungerle allo
stesso posto.

- [ ] **Step 5: commit**

```bash
git add docker/docker-compose.yml .github/workflows/images.yml
git commit -m "feat(loki): il servizio in locale, con l'alias che tiene una sola config"
```

---

## Task 6: la pipeline `logs` nel Collector

**Dipende dal Task 2.**

**Files:**
- Modify: `docker/otel-collector-config.yaml`

- [ ] **Step 1: l'allow-list #1, sotto `processors:`**

```yaml
  # ALLOW-LIST #1, sui LOG record. Stessa forma e stessa ragione di quella sulle
  # metriche: si tiene cio' che e' noto-sicuro e si scarta il resto, cosi' un
  # attributo che una versione futura inventa e' una chiave mancante e non una
  # fuga. Una delete-list qui fallirebbe aperta su ogni novita'.
  #
  # Le chiavi sono l'ESITO DELLA MISURA del Task 2, non una supposizione.
  transform/log-allowlist:
    log_statements:
      - context: log
        statements:
          - keep_keys(log.attributes, [<ESITO DEL TASK 2>])
```

- [ ] **Step 2: l'exporter, sotto `exporters:`**

```yaml
  # OTLP nativo verso Loki: `POST /otlp/v1/logs` e' l'endpoint documentato dal
  # fornitore. NON si usa il lokiexporter, che e' deprecato a monte.
  otlphttp/loki:
    logs_endpoint: http://loki.railway.internal:3100/otlp/v1/logs
```

- [ ] **Step 3: la pipeline, sotto `service.pipelines:`**

```yaml
    # NUOVA E SEPARATA, non un ramo di `metrics`. L'exporter `prometheus` supporta
    # il solo segnale metriche, quindi metterlo qui fa RIFIUTARE L'AVVIO al
    # Collector — e' scritto in CLAUDE.md fra le cose da non fare mai, e da oggi
    # c'e' anche un gate (Task 8) invece del solo commento.
    logs:
      receivers: [otlp]
      # Stesso ordine della pipeline metriche, per le stesse ragioni: il freno per
      # primo, l'allow-list prima del batch cosi' niente di identificante viene mai
      # tenuto in un buffer o passato a un exporter.
      processors: [memory_limiter, transform/log-allowlist, batch]
      exporters: [otlphttp/loki]
```

- [ ] **Step 4: aggiornare il commento che dice che i log non hanno consumatore**

Nel blocco `pipelines:` c'è un commento che dice *"Claude Code's log/event stream has
no consumer in Phase 1"*. Adesso ce l'ha. Va riscritto, non lasciato: un commento vero
quando è stato scritto e falso adesso è la cosa che questo repository corregge più
spesso.

- [ ] **Step 5: validare la config con il binario vero**

```bash
docker run --rm -v "$PWD/docker/otel-collector-config.yaml:/c.yaml:ro" \
  -e OTLP_INGEST_TOKEN=finto \
  otel/opentelemetry-collector-contrib:0.158.0-386@sha256:93e4793719dd55d0d9499328e7b45af219116cc9d1bcb95df5b3a0f8cade831d validate --config=file:/c.yaml
```

Atteso: exit 0, nessun output d'errore.

- [ ] **Step 6: commit**

```bash
git add docker/otel-collector-config.yaml
git commit -m "feat(collector): una pipeline logs separata, con l'allow-list scritta dalla misura"
```

---

## Task 7: il datasource Grafana — indipendente, parallelizzabile

**Files:**
- Create: `docker/grafana/provisioning/datasources/loki.yml`

- [ ] **Step 1: il file**

```yaml
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    # Stesso hostname in locale e su Railway (vedi gli alias di rete nel compose).
    url: http://loki.railway.internal:3100
    # isDefault resta a Prometheus: i tre numeri pubblici passano da li', e un
    # datasource di default sbagliato fa aprire i pannelli nuovi sulla fonte
    # sbagliata senza dire niente.
    isDefault: false
```

- [ ] **Step 2: verificare il provisioning con Grafana vero**

```bash
docker run -d --rm --name gtest -p 3999:3000 \
  -e GF_SECURITY_ADMIN_PASSWORD=testtesttest \
  -v "$PWD/docker/grafana/provisioning:/etc/grafana/provisioning:ro" \
  -v "$PWD/docker/grafana/dashboards:/etc/grafana/dashboards:ro" \
  grafana/grafana:13.1.4
until curl -sf http://localhost:3999/api/health >/dev/null; do sleep 2; done
curl -s -u admin:testtesttest http://localhost:3999/api/datasources | python3 -m json.tool | grep -E '"name"|"type"|"isDefault"'
docker logs gtest 2>&1 | grep -i "error" | grep -i datasource || echo "nessun errore sui datasource"
docker stop gtest
```

Atteso: due datasource, `Prometheus` (`isDefault: true`) e `Loki` (`false`).

- [ ] **Step 3: commit**

```bash
git add docker/grafana/provisioning/datasources/loki.yml
git commit -m "feat(grafana): il datasource Loki, con Prometheus che resta il default"
```

---

## Task 8: i gate in CI

**Dipende dai Task 3 e 6.**

**Files:**
- Modify: `.github/workflows/images.yml`

- [ ] **Step 1: il gate che `CLAUDE.md` chiedeva e nessuno aveva scritto**

Aggiungere un job step in `images.yml`, accanto agli altri gate sulle configurazioni:

```yaml
      # CLAUDE.md dice "Never put the `prometheus` exporter in a `logs` pipeline" —
      # il Collector si RIFIUTA di partire. Era scritto in un file che nessun gate
      # legge, e fino alla Fase 1.5 non c'era nemmeno una pipeline logs in cui
      # sbagliare. Adesso c'e'. Un commento non e' un gate.
      - name: la pipeline logs non contiene l'exporter prometheus
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import sys, yaml
          cfg = yaml.safe_load(open("docker/otel-collector-config.yaml"))
          logs = (cfg.get("service", {}).get("pipelines", {}) or {}).get("logs")
          if logs is None:
              print("::error::la pipeline `logs` non esiste piu' in docker/otel-collector-config.yaml.")
              print("::error::Se e' stata rimossa apposta, va tolto anche questo gate nello stesso commit.")
              sys.exit(1)
          cattivi = [e for e in logs.get("exporters", []) if e == "prometheus" or e.startswith("prometheus/")]
          if cattivi:
              print(f"::error::la pipeline logs esporta verso {cattivi}: l'exporter prometheus supporta il solo segnale metriche.")
              print("::error::Il Collector NON PARTE, quindi si fermano anche le metriche e i tre numeri pubblici si congelano.")
              sys.exit(1)
          print(f"ok: la pipeline logs esporta verso {logs.get('exporters')}")
          PY
```

- [ ] **Step 2: il gate sull'allow-list dei log**

```yaml
      # Stessa famiglia del gate sulle label delle metriche. La forma da
      # sorvegliare non e' l'elenco delle chiavi — quello cambia con il client — ma
      # che sia un ALLOW-LIST: `keep_keys` nel Collector e `ignore_defaults: true`
      # piu' un `drop` finale in Loki. Un delete-list fallisce aperto su ogni
      # attributo che una versione futura inventa, ed e' esattamente la forma che
      # questo progetto ha gia' rifiutato una volta sulle metriche.
      - name: i log passano da due allow-list, non da una delete-list
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import sys, yaml
          fallimenti = []

          cfg = yaml.safe_load(open("docker/otel-collector-config.yaml"))
          proc = cfg.get("processors", {})
          allow = [n for n in proc if n.startswith("transform/log-allowlist")]
          if not allow:
              fallimenti.append("il Collector non ha piu' un processor transform/log-allowlist")
          else:
              testo = yaml.safe_dump(proc[allow[0]])
              if "keep_keys" not in testo:
                  fallimenti.append("transform/log-allowlist non usa keep_keys: e' diventata una delete-list")
              pipeline = cfg["service"]["pipelines"].get("logs", {})
              if allow[0] not in pipeline.get("processors", []):
                  fallimenti.append(f"{allow[0]} esiste ma NON e' nella pipeline logs: e' come non averlo")

          loki = yaml.safe_load(open("docker/loki.yaml"))
          otlp = (loki.get("limits_config", {}) or {}).get("otlp_config", {}) or {}
          if otlp.get("resource_attributes", {}).get("ignore_defaults") is not True:
              fallimenti.append("loki.yaml non ha ignore_defaults: true: il DEFAULT di Loki mette le resource attribute nell'INDICE")
          azioni = [a.get("action") for a in (otlp.get("log_attributes") or [])]
          if "drop" not in azioni:
              fallimenti.append("loki.yaml non ha un'azione drop finale sui log_attributes: l'allow-list non si chiude")

          if fallimenti:
              for f in fallimenti:
                  print(f"::error::{f}")
              sys.exit(1)
          print("ok: due allow-list, entrambe chiuse")
          PY
```

- [ ] **Step 3: provare ENTRAMBI i gate rossi, rompendo la realtà**

Non si cambia l'atteso: si rompe il file che il gate guarda, si esegue lo script
estratto dal YAML, si ripristina.

```bash
cp docker/otel-collector-config.yaml /tmp/coll.bak
cp docker/loki.yaml /tmp/loki.bak

# 1. exporter prometheus nella pipeline logs
python3 - <<'PY'
import yaml
c = yaml.safe_load(open('docker/otel-collector-config.yaml'))
c['service']['pipelines']['logs']['exporters'].append('prometheus')
yaml.safe_dump(c, open('docker/otel-collector-config.yaml','w'))
PY
# eseguire il gate dello Step 1 -> atteso exit 1
cp /tmp/coll.bak docker/otel-collector-config.yaml

# 2. keep_keys diventa delete_key
sed -i '' 's/keep_keys/delete_key/' docker/otel-collector-config.yaml
# eseguire il gate dello Step 2 -> atteso exit 1
cp /tmp/coll.bak docker/otel-collector-config.yaml

# 3. ignore_defaults tolto da Loki
python3 - <<'PY'
import yaml
c = yaml.safe_load(open('docker/loki.yaml'))
del c['limits_config']['otlp_config']['resource_attributes']['ignore_defaults']
yaml.safe_dump(c, open('docker/loki.yaml','w'))
PY
# eseguire il gate dello Step 2 -> atteso exit 1
cp /tmp/loki.bak docker/loki.yaml

git diff --stat   # deve essere VUOTO
```

- [ ] **Step 4: provarli verdi sullo stato vero**

Eseguire entrambi gli script sui file ripristinati. Atteso: exit 0 e le due righe `ok:`.

- [ ] **Step 5: nessuno step duplicato**

```bash
python3 -c "
import collections, yaml
w = yaml.safe_load(open('.github/workflows/images.yml'))
for j, job in w['jobs'].items():
    nomi = [s.get('name') for s in job.get('steps',[]) if s.get('name')]
    dup = [n for n,c in collections.Counter(nomi).items() if c>1]
    assert not dup, (j, dup)
print('nessuno step duplicato')"
```

- [ ] **Step 6: commit**

```bash
git add .github/workflows/images.yml
git commit -m "ci(loki): due gate sulle allow-list dei log, e uno sull'exporter che non deve stare li'"
```

---

## Task 9: la prova che conta

**Dipende dai Task 3, 5, 6.** È l'unico test di questo piano che verifica una proprietà
di sicurezza invece di una forma.

**Files:**
- Modify: `docs/LOCAL_DRY_RUN.md`

- [ ] **Step 1: avviare lo stack completo in locale**

```bash
cd docker
set -a; . ./.env; set +a
docker compose up -d --build otel-collector loki grafana
```

Le credenziali R2 in `.env` devono essere quelle di un bucket **di prova**, non di
produzione. Se non ci sono, il Task 9 si esegue dopo che Marco lo ha creato.

- [ ] **Step 2: mandare un log record con dentro un'identità**

```bash
set -a; . ./.env; set +a
curl -sS -X POST http://localhost:4318/v1/logs \
  -H "Authorization: Bearer $OTLP_INGEST_TOKEN" \
  -H 'Content-Type: application/json' \
  -d '{"resourceLogs":[{"resource":{"attributes":[
        {"key":"service.name","value":{"stringValue":"claude-code"}},
        {"key":"user.email","value":{"stringValue":"NON-DEVE-ARRIVARE@example.com"}}]},
      "scopeLogs":[{"logRecords":[{
        "timeUnixNano":"'"$(date +%s)000000000"'",
        "body":{"stringValue":"prova"},
        "attributes":[
          {"key":"session.id","value":{"stringValue":"prova-sessione"}},
          {"key":"user.id","value":{"stringValue":"NON-DEVE-ARRIVARE-NEMMENO"}}]}]}]}]}'
sleep 15
```

- [ ] **Step 3: verificare che NON sia arrivato — in nessuna delle tre forme**

```bash
# a) non e' una label di stream
curl -s "http://localhost:3100/loki/api/v1/labels" | tee /tmp/labels.json
grep -iE "user_email|user_id|user\.email|user\.id" /tmp/labels.json && echo "FALLITO: e' nell'indice" || echo "ok: non e' una label"

# b) non e' nel record ne' nella structured metadata
curl -sG "http://localhost:3100/loki/api/v1/query_range" \
  --data-urlencode 'query={service_name="claude-code"}' \
  --data-urlencode "start=$(($(date +%s)-300))000000000" \
  --data-urlencode "end=$(date +%s)000000000" > /tmp/query.json
grep -c "NON-DEVE-ARRIVARE" /tmp/query.json

# c) il record utile c'e' comunque: un allow-list che scarta tutto e' inutile
grep -c "prova-sessione" /tmp/query.json
```

Atteso: **(a) nessuna occorrenza · (b) `0` · (c) almeno `1`.**

Il punto (c) non è decorativo: senza, un allow-list rotto che scarta *ogni* attributo
supererebbe (a) e (b) e sembrerebbe un successo.

- [ ] **Step 4: la trappola già pagata su questo progetto**

Questo test usa un payload **sintetico**, e su questo repository un payload sintetico
ha già confermato una query e mentito. Non sostituisce il dry run col client vero del
Task 2: verifica che le allow-list scartino ciò che gli si mette davanti, non che il
client mandi solo quello. Scriverlo in `LOCAL_DRY_RUN.md` accanto al test.

- [ ] **Step 5: documentare la prova e i suoi limiti**

Aggiungere a `docs/LOCAL_DRY_RUN.md` una sezione con i comandi sopra, l'esito atteso e
il paragrafo dello Step 4.

- [ ] **Step 6: commit**

```bash
git add docs/LOCAL_DRY_RUN.md
git commit -m "docs(loki): la prova che l'identita' non arriva, e cosa NON prova"
```

---

## Task 10: la documentazione

**Ultimo.** I file che affermano qualcosa vanno aggiornati nello stesso ciclo, non dopo.

**Files:**
- Modify: `docs/DECISIONS.md`, `SECURITY.md`, `CLAUDE.md`, `README.md`, `docs/BLOCKERS.md`

- [ ] **Step 1: `docs/DECISIONS.md`**

Nella sezione su Loki (cercare `**Grafana Loki: evaluated 2026-07-29`), aggiungere in
coda che la Fase 1.5 è stata implementata, con: la data, le quattro decisioni del
design pass, e **il costo misurato** dopo una settimana — quest'ultimo va aggiunto
*dopo*, non stimato adesso.

- [ ] **Step 2: `SECURITY.md`**

Due modifiche:
1. Nella sezione sugli hostname, aggiungere che `otel.` accetta ora **due** percorsi
   (`/v1/metrics` e `/v1/logs`) e che `/v1/logs` è asserito da `smoke.yml`.
2. Nella tabella dei controlli fuori da git, la riga della regola WAF va aggiornata:
   copre due percorsi, non uno.
3. Aggiungere che **Loki non ha hostname del Tunnel**, con la ragione giusta (nessuna
   rotta), non con quella falsa già corretta per Prometheus.

- [ ] **Step 3: `CLAUDE.md`**

La riga *"Never put the `prometheus` exporter in a `logs` pipeline"* adesso ha un gate:
va detto, perché una regola sorvegliata e una scritta si leggono diversamente.
Aggiungere Loki all'elenco dei servizi (da cinque a sei) e la nota che i suoi chunk
stanno su R2, quindi un volume Railway pieno non se li porta via.

- [ ] **Step 4: `README.md`**

Aggiornare il conteggio dei servizi e la sezione sui gate con i due nuovi.

- [ ] **Step 5: `docs/BLOCKERS.md`**

La voce su Loki passa da "due trigger, il secondo scattato" a **chiusa**, con il
rimando alla spec e al piano. E va aggiunta la riga di §10 della spec: **riaprire
`/v1/logs` è una modifica alla superficie raggiungibile e va richiusa a mano se Loki
esce di scena**, altrimenti resta aperta una rotta che non serve più a nessuno.

- [ ] **Step 6: commit**

```bash
git add docs/ SECURITY.md CLAUDE.md README.md
git commit -m "docs(loki): la Fase 1.5 e' spedita, e i file che la dichiaravano assente lo dicono"
```

---

## Cosa resta a Marco, e non è delegabile

| passo | dove | quando |
|---|---|---|
| estendere la regola WAF a `POST /v1/logs` | dashboard Cloudflare | **prima di tutto** (Task 0) |
| creare il bucket R2 e le credenziali S3 | dashboard Cloudflare | prima del Task 9 |
| creare il servizio `loki` su Railway | dashboard Railway | prima del primo deploy |
| impostare le quattro variabili `LOKI_R2_*` | variabili di servizio Railway | insieme al servizio |

**Nessuna di queste va fatta da un agente**: gli hook non scattano nei subagenti, e
sono azioni verso l'infrastruttura vera.

---

## Verifica finale, prima di dire "fatto"

- [ ] Tutti i gate verdi su `main`, compresi i due nuovi
- [ ] Il deploy di `loki` su Railway è `SUCCESS` **e il suo `commitHash` è quello atteso**
      — `SKIPPED` e `REMOVED` sono i due stati che significano "non è arrivato"
- [ ] I tre numeri pubblici rispondono ancora: `curl -s https://marcobellingeri.dev/api/agentic-status`
- [ ] `smoke.yml` verde dopo il merge, lanciato a mano con `workflow_dispatch`
- [ ] Grafana mostra i log di una sessione vera, filtrati per `session.id`
- [ ] Dopo una settimana: il costo misurato scritto in `DECISIONS.md`, non la stima
