# Real secrets — never committed (only the .example file is)
docker/.env

# Python
__pycache__/
.pytest_cache/
.venv/
coverage.xml
.coverage

# mutmut working copy
mutants/

# Output di graphify: artefatto rigenerabile (309 nodi da 42 file). Il valore
# durevole sta in docs/DECISIONS.md, non nel grafo che lo indicizza.
graphify-out/
