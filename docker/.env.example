# Copy to docker/.env ON THE VPS and fill in real values. Never commit .env.
# SENTRY_DSN is optional: without it the status API's error capture is a no-op.

CLOUDFLARE_TUNNEL_TOKEN=
GRAFANA_ADMIN_PASSWORD=
STATUS_API_TOKEN=
OTLP_INGEST_TOKEN=
SENTRY_DSN=
