{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "railway/cloudflared/Dockerfile",
    "watchPatterns": [
      "railway/cloudflared/**"
    ]
  },
  "deploy": {
    "restartPolicyType": "ALWAYS"
  }
}
